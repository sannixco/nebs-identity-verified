[project]
name = "nebs-identity-verified"
authors = []
description = ""
telemetry = true
requirements = []
cache_dir = "/home/runner/workspace/nebs-identity-verified/./.requirements"
boot_contracts = ["pox", "costs-v2", "bns"]
[contracts.nebs-identity-verified]
path = "contracts/nebs-identity-verified.clar"

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
